a graphical prompter is available but SSH_ASKPASS is not wired into this shell — ssh will prompt for passphrases on the raw terminal instead of routing them through the wallet; likely because this shell was never a login shell, so /etc/profile.d (or ~/.bash_profile) was never sourced — opening a plain new terminal tab won't fix it unless that terminal also starts a login shell; re-source your profile directly, or start a login shell explicitly (e.g. bash -l)
