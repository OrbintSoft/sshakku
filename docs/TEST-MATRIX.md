# Test matrix

Every combination of OS, secret-backend integration, session/display
environment, and failure scenario that matters to a real SSHakku user, and
whether an **integration** test exercises it -- a real `ssh-agent`/
`ssh-add`, a real secret-store daemon, a real shell, not a mock. A case
covered only by a unit test with a faked/mocked dependency counts as not
covered here: that's a different, already-answered question (does the logic
work in isolation), not this document's question (does the real thing work
end to end). Cases where an integration test wouldn't add anything over the
existing unit coverage are left out of this document entirely. Whether a
covered case runs on every merge or only on demand is a separate concern,
tracked by that case's own badge, not by this document.

Use this to see what's actually exercised versus assumed to work, and to
plan new integration tests where a cell shows a gap.

## Legend

| Mark | Meaning |
| --- | --- |
| ✅ | Covered by an integration test. |
| ⚠️ | Partly covered — the cell says which part is and which is not. |
| ❌ | Not integration-tested. |
| — | Not applicable (the combination doesn't exist in practice). |

## Secret store × OS

Which wallet a user can actually choose, on which platform, and what covers it
there. This is the first table on purpose: a user picks *KeePassXC*, not "the
secret-service backend", and how SSHakku reaches it is an implementation
detail that does not always carry across platforms. Organised the other way
round — by the mechanism — a wallet that is offered on one OS and absent on
the other has no cell to be missing from, and the gap becomes invisible.

| Wallet | Linux | macOS |
| --- | --- | --- |
| KDE Wallet (`ksecretd`/`kwalletd6`) | ✅ via `secret-service`, `kde.Dockerfile` | — (no D-Bus session bus or Secret Service on macOS) |
| GNOME Keyring | ✅ via `secret-service`, `gnome-keyring.Dockerfile` | — (same) |
| KeePassXC | ✅ all three routes: `secret-service` (`keepassxc.Dockerfile`, real GUI under Xvfb), `cli` (`TestKeePassXCCLIRealDatabase` — a real `keepassxc-cli` store/read/replace/list/forget round trip against a throwaway database, F4/F5/F6/F9) and `native` (`TestKeePassXCNativeFullRound` — the real binary and a dedicated `ssh-agent` against a real KeePassXC over its local protocol, F4/F5/F6/F9/F23) | ✅ two routes against the `brew --cask keepassxc` build on `macos-latest`: `cli` (`TestKeePassXCCLIRealDatabase`, F4/F5/F6/F9/F22) and `native` (`TestKeePassXCNativeFullRound`, F4/F5/F6/F9/F23 — the test starts KeePassXC itself, since nothing on that platform provides a running one). `secret-service` has no cell here: macOS offers no such API. Two steps a person normally takes are taken without one — the database is opened with `--pw-stdin` holding the stream open, and the association is written into the database rather than approved in a dialog, both being preconditions of the route rather than the promises under test |
| 1Password (`op` CLI) | ✅ `TestOnePasswordBackendRealAccount`, `onepassword-real-account.yml` — a throwaway vault created for the run, stored into, read back, listed and deleted, then removed whatever the outcome, against a real account (F4/F5/F6/F9, and F17 for the wallet being the chosen one) | ✅ the same test on `macos-latest`, in the same matrix, verifying the same promises there — plus F26, since macOS is a platform that declares this wallet. It took 13s against a real account, not the 0.00s of a skip. Both legs go through `test/onepassword-real-account.sh`, which requires the test's own `--- PASS:` line: `go test -run` exits 0 when the test skips itself and again when the name matches nothing, so before it a green job and a job that reached no wallet at all were the same result. Made to fail by that skipped run |
| Bitwarden (`bw` CLI) | ✅ `TestBitwardenBackendRealAccount` against a self-hosted Vaultwarden, `desktop-stack.yml` — an item stored, read back, listed, updated in place and deleted, each call unlocking and locking for itself (F4/F5/F6/F9, and F17 for the wallet being the chosen one), plus an item SSHakku did not store that `List` must not report (F27/F32). It goes through `test/bitwarden-real-account.sh`, which requires the test's own `--- PASS:` line: `go test -run` exits 0 when the test skips itself, so before it a green job and a job that reached no vault at all were the same result | ✅ the same test on `macos-latest`, verifying the same promises there — plus F26, since macOS is a platform that declares this wallet. `bw` and the test binary are native; only the server is Linux, in a container on a machine Colima emulates on the runner, since Apple's hypervisor is unavailable on a runner that is itself a virtual machine. It took 114s against that server, not the 0.00s of a skip, and goes through the same guard |
| macOS Keychain | — (Security.framework is macOS-only) | ✅ `TestDarwinKeychainClientRealRoundTrip` — a live `Add`/`Find`/`Update`/`Delete`/`List` round trip against a throwaway default keychain, plus the whole shell suite (`make test-bats`) driving it as the default backend |

A ❌ here means the promise is not covered on that platform, whether because
nothing tests it or because nothing implements it; the cell says which. A `—`
means the wallet cannot exist there at all.

## Secret-service backend × session/display environment

Linux only, and a second dimension rather than a second list of wallets:
which desktop session the daemon runs under. Whether each of these wallets is
available on a given OS at all is the table above.

`ksecretd`/`kwalletd6`, `gnome-keyring-daemon`, and KeePassXC all implement
the same `org.freedesktop.secrets` D-Bus API and are driven through the one
`SecretServiceBackend`/`TestSecretServiceBackendRealDaemon` test; only the
container session around it differs.

| Session | X11 | Wayland | No display |
| --- | --- | --- | --- |
| KDE (`ksecretd`/`kwalletd6`) | ✅ `kde.Dockerfile`, `SSHAKKU_SESSION_SCRIPT=kde-x11-session.sh` (F4, F5, F9) | ✅ `kde.Dockerfile`, `SSHAKKU_SESSION_SCRIPT=kde-wayland-session.sh` (F4, F5, F9) | ✅ `kde.Dockerfile` (F4, F5, F9) |
| GNOME (`gnome-keyring-daemon`, desktop session) | ✅ `gnome-keyring.Dockerfile` (F4, F5, F9), and `gnome-keyring-fix-compartment-scenario.sh` for making the compartment on a screen, dialog and all (F42) | ✅ `gnome-keyring.Dockerfile`, `SSHAKKU_SESSION_SCRIPT=gnome-keyring-wayland-session.sh` (F4, F5, F9) | — |
| GNOME Keyring, headless-daemonized (no session/display at all) | — | — | ✅ `SSHAKKU_SESSION_SCRIPT=gnome-keyring-headless-session.sh`, twice over: the round trip against a compartment made in a login that had a screen, via `run-headless-wallet.sh` (F4, F5, F9, F39), and `gnome-keyring-headless-scenario.sh` for the machine where it was never made — the keys still load, the asking comes back every time, and a session with nobody to ask is not held up (F39, F40). `gnome-keyring-fix-compartment-scenario.sh` runs here too, for the other half of F42: a repair that cannot be performed here is said so rather than attempted |
| KeePassXC (standalone, any desktop) | ✅ `keepassxc.Dockerfile` | ✅ `keepassxc.Dockerfile`, `SSHAKKU_SESSION_SCRIPT=keepassxc-wayland-session.sh` | — |

KDE is the one wallet here that needs no display of any kind: PAM opens the
wallet as part of the login, so ksecretd is never asked to draw anything and
the round trip runs in a session that has no screen at all. Its three ✅s are
the same image and the same test, differing only in the login built around
them — no display, a compositor, or an X server.

What makes those three genuinely different runs is that the daemon is subject
to the session too, not only SSHakku: ksecretd draws on whichever display its
login provides, and the Qt platform plugin it maps says which one it took
(`libqoffscreen.so`, `libqwayland.so`, `libqxcb.so` respectively). Until the
X11 session was built they were not different runs at all — `kde.env`, which
PAM reads into the daemon's environment, pinned every one of them to the
offscreen platform, so the daemon never saw the screen its session had. The
control that shows this now holds: with the X server taken away, the X11
session fails because ksecretd never registers on the bus; with the pin put
back, that same displayless session passes.

The `—`s in the two desktop-session rows are what those rows are: a desktop
session that has no display is not a desktop session. What GNOME Keyring's own
daemon needs a display for is narrower than it looks, and the headless row is
where that was established — it runs with no screen at all quite happily, and
serves a compartment already on disk. What cannot be done without one is *making*
that compartment: gnome-keyring answers `CreateCollection` with a prompt every
time, and the prompter has nowhere to draw it. So the headless row is covered
twice, once for each side of that line, and the second run is the one that says
what a user gets on a machine where the compartment was never made.

XFCE has no native provider of its own -- it would only ever host GNOME
Keyring or KeePassXC, both already rows above -- so it isn't listed separately
unless a KeePassXC-under-XFCE test is actually worth adding later.

There is a Wayland session to run in now — sway on wlroots' headless backend,
with no X server and no Xwayland, shared by `wayland.Dockerfile` and any other
image that starts it — and the passphrase-prompt rows further down are driven
in it as well.

GNOME Keyring's cell was the one that had something to teach. Its collection is
created through a dialog, and that dialog cannot be answered from the keyboard
at all: `libgcr-ui` grabs the seat before it accepts input, and a seat with no
input device has nothing to grant, so the window draws, keeps focus, and ignores
every key — while zenity, the same toolkit in the same session, takes what is
typed into it happily. The session is therefore given a pointer device that
never sends an event, purely so the seat has one, and the dialog is clicked
through the compositor's own cursor at the same two points the X11 script
clicks. The container stays unprivileged and mounts no `/dev/input`: it makes
the single node it uses and can open nothing else, so the machine running the
test keeps its own keyboard to itself.

KeePassXC is answered the same way, and its session is where the Wayland side
pays off rather than costs: the compositor names every window, so each thing the
app asks is recognised by what it says it is — "Create a new KeePassXC
database…", "Save database as" — instead of by whether the last click landed
within the sleep the script allowed. Its buttons are pressed by position, but
relative to the window that owns them.

## Backend round trips

What each backend's own store/lookup/delete cycle is exercised by, once the
table above has established where that backend is available at all.

| Backend | Covered by |
| --- | --- |
| Keychain | `TestDarwinKeychainClientRealRoundTrip` — a live round trip through `DarwinKeychainClient` (`Add`/`Find`/`Update`/`Delete`/`List`, including the duplicate-add and update-missing error paths) against a throwaway default keychain the `test-macos` job stands up first (`test/macos-keychain-setup.sh`), so the runner's login keychain is never touched |
| Secret Service | `TestSecretServiceBackendRealDaemon`, against each of the three daemons in its own container |
| KeePassXC (`cli` route) | `TestKeePassXCCLIRealDatabase` — a real `keepassxc-cli` against a throwaway database it creates itself: store, read back, replace in place, list, forget, and a final lookup that must miss (F4, F5/F6, F9). Also the only check that `keepassxc-cli` still takes the database password on standard input, which it offers no documented flag for |
| KeePassXC (`native` route) | `TestKeePassXCNativeFullRound` — not a backend round trip but the user's own scenario: the real binary asks once on a real terminal, saves the passphrase in a real KeePassXC over its local protocol, then loads the key into a dedicated `ssh-agent` with nothing typed, again after the key expires, and reports that this wallet cannot be forgotten from (F4, F5, F6, F9, F23). The silent steps run with a terminal attached, so a regression that starts asking is caught rather than passing for want of one |
| 1Password (`op` CLI) | `TestOnePasswordBackendRealAccount`, `onepassword-real-account.yml` |
| Bitwarden (`bw` CLI) | `TestBitwardenBackendRealAccount`, `desktop-stack.yml` — against a self-hosted Vaultwarden on both platforms: inside the container on Linux, and from macOS against a container the runner emulates a machine for, so the CLI under test is the platform's own |

## Install and uninstall methods

| Case | Linux | macOS |
| --- | --- | --- |
| System-wide (`make install`/`uninstall`) | ✅ `test/linux-install-smoke.sh` | ✅ `test/macos-install-smoke.sh` |
| Per-user (`make install-user`/`uninstall-user`) | ✅ `test/linux-install-smoke.sh` | ✅ same script |
| Non-login shell wiring, opt-in (`WIRE_BASHRC=1`/`WIRE_ZSHRC=1`) (F20) | ✅ `test/linux-install-smoke.sh` (both the `bashrc.d` drop-in and the fallback-file shape) | ✅ `test/macos-install-smoke.sh` — the system-wide `/etc/zshrc` marker block and both per-user shapes (a drop-in into an existing `~/.zshrc.d`, a marker block in `~/.zshrc` when there is none), each checked to leave the login-shell wiring in place and to be removed again on uninstall |
| User bindir put on `PATH` from the hook (`install-user`, default; `WIRE_PATH=0` opts out) | ✅ `test/linux-install-smoke.sh` | ✅ `test/macos-install-smoke.sh` |

## Agent lifecycle and recovery scenarios

| Scenario | Linux | macOS |
| --- | --- | --- |
| Fresh start, no agent running yet | ✅ `TestEnsureAgentRealClean` | ✅ same |
| Agent already running and healthy, reused rather than restarted | ✅ `TestEnsureAgentRealHealthyReuse` | ✅ same |
| Agent reachable with zero keys loaded, still treated as healthy | ✅ `TestEnsureAgentRealReachableButEmptyIsHealthy` | ✅ same |
| Agent process killed (SIGKILL), stale socket left behind, recovered on next run | ✅ `TestEnsureAgentRealZombie` | ✅ same |
| Agent stopped gracefully (SIGTERM) after being registered, socket removed rather than left stale | ✅ `TestEnsureAgentRealGracefulStopRemovesSocket` | ✅ same |
| A foreign `ssh-agent` started before SSHakku ever runs, adopted rather than killed | ✅ `TestEnsureAgentRealForeignAdopted` | ✅ same |
| One dead agent of ours plus multiple healthy foreign agents | ✅ `TestEnsureAgentRealDisasterReapsAndAdoptsLowestPID` | ✅ same |
| The wallet named in the report is the one that would be opened (F25, F26) | ✅ `TestDoctorNamesTheWalletThatWouldBeUsed` and `TestDoctorRefusesAWalletThisSystemHasNot` drive the whole command over a real config file; `TestSecretBackendChoicesOnLinux` pins that the Keychain is not a name this system offers | ✅ same, plus `TestDoctorReportOnAnUnconfiguredMachine` and `TestSecretBackendChoicesOffLinux` — the case as it was reported, where doctor named the Secret Service and asked for a D-Bus session bus no Mac has |
| The report says when the configured wallet can hold nothing here, and looking never changes it (F25, F41) | ✅ Driven through the real binary on the `gnome-keyring` image in a session with no screen: the report names the wallet, says the compartment is not there and that this session has no screen to create it on, and says it among the findings where a user looks for problems. On a bus this scenario starts for itself, with nothing owning `org.freedesktop.secrets` and nothing that could be started, the report still comes and names the wallet as the thing that is not there. Against the previous build both of those said `session bus: found` and nothing else — a machine where no passphrase can ever be saved was reported entirely healthy, which is how it was found. The wallet holds the same collections after the report as before it, and a daemon frozen with SIGSTOP costs the report one bounded pause instead of the report itself. `TestLookForCollection` pins the look against a real Secret Service on a private bus: made to fail by resolving the compartment the ordinary way, which created one (`looking created 1 collection(s)`), and by dropping the call bound, which turned the frozen case into a 30-second wait. `TestSecretServiceRequirementsFromALook` and `TestDoctorReportsAWalletThatCanHoldNothingHere` pin what the report makes of each answer | ✅ the half that decides what to say runs in the macOS job unchanged — it takes the look as data and names no D-Bus type, which is why it is checkable from a machine that has no bus. The other half cannot exist there: macOS reaches its keychain through the system, so there is no daemon that could be absent and no compartment a dialog has to create |
| The compartment the wallet keeps passphrases in is made by `--fix`, and only where it can be (F42, F14) | ✅ `gnome-keyring-fix-compartment-scenario.sh`, the real binary in both sessions of the `gnome-keyring` image, with the wallet's own dialog answered from the keyboard. On a screen: the report says the compartment is not there and offers `--fix`, `--fix` says what it made, the report that follows lists the compartment as present, and the bus — asked directly, not through SSHakku — holds a collection it did not hold before. With no screen: `--fix` says it could not, says what it would take, the report still comes back whole, and the wallet holds exactly what it held before. Made to fail by running the same scenario against the previous build, where the screen half failed five assertions (the report never offered the repair, nothing was made, and the report afterwards had no wallet section at all) and the no-screen half three; the "not there" assertion was made to fail on its own by the build that called a compartment it had not got `found`. `TestDoctorMakesTheCompartment` pins each sentence of the promise, including a wallet that refuses, which exits non-zero rather than reporting a repair that did not happen | — the Keychain has no compartment to make: entries go into the login keychain the account already has, so there is nothing to create and no dialog to answer. The decision is still checked from the macOS job — the case for a system with nothing of the kind to make is one of `TestDoctorMakesTheCompartment`'s own, and it runs there unchanged |
| `doctor` names what launched an agent (F13) | ✅ `TestProcfsAncestryParent`, `TestStartedBy`, `TestLauncherLabel` and `TestGatherReparentedToInitCgroupFallback` — the procfs walk, the systemd/desktop launcher table, and the cgroup fallback for a daemon reparented to init | ✅ `TestPSAncestryParent`, `TestParsePS`, `TestLauncherLabel`, `TestStartedByOnDarwin` — the `ps` walk and the launchd/loginwindow/terminal table. Before these, macOS had no ancestry source at all: `/proc` was read there too, so every agent was attributed to nobody |
| A graphical passphrase prompt is raised where one can be, and only there (F4, F29) | ✅ Driven through the real binary in a disposable image with a display server and no KDE tooling on it: a dialog titled `SSHakku` appears with no controlling terminal at all, is typed into, and the answer reaches the product. The same scenario against the previous build produced no dialog at any point — the promise held only where kdialog happened to be installed, which is how it was found. `TestGraphicalPromptOnADesktopWithoutKDE` and `TestGraphicalPromptWithOnlyZenity` pin a session that has only pinentry and only zenity, `TestGraphicalPrompterWithASessionAndKDialog` that KDE keeps what it had, `TestNoGraphicalPrompterWithNoDisplayServer` the headless answer, and `TestPinentryPrompt`, `TestKDialogPrompt`, `TestZenityPrompt` each dialog itself — pinentry against a real process speaking the real protocol. What the container run cannot show is the key reaching the agent: its kernel keyring refuses the cross-process handoff, which is that environment, not the prompt | ⚠️ `TestGraphicalSession` and `guiprompt_darwin_test.go` cover the decision — Aqua gets the dialog, an SSH login and a single-user boot get the terminal — with a `launchctl` on PATH answering as the real one does. The hosted runner's own session reports Aqua, so `test/bats/shell-plumbing.bats` does raise a real dialog with nobody in front of it, and shows sshakku giving up on it inside its budget (F21). What no job can show is the window being usable: nobody is at that screen to read or answer it. That half waits on a run on a real Mac |
| A graphical session whose `pinentry` is one of GnuPG's console builds (F29, F37) | ✅ Real binary in a disposable image with a display server, zenity, and `pinentry-curses` as the only pinentry, and no controlling terminal: zenity's window appears and is typed into. The same scenario against the previous build printed `NO DIALOG EVER APPEARED` and logged `pinentry ... Inappropriate ioctl for device`, then `no terminal available` — a user with a screen and a working dialog beside them was asked nowhere at all, which is how it was found. With `gui_prompter = "pinentry"` on that same machine the log reads `gui_prompter names pinentry, which is not installed, or is a build that draws on a terminal rather than on a screen; asking on the terminal`: the named one is never substituted, and the sentence is true of a pinentry that is installed. `TestGraphicalPromptWhereTheOnlyPinentryIsAConsoleOne` pins which dialog is chosen, `TestPinentryAvailable` that `curses` and `tty` are not dialogs while `gtk2:curses` is and an answer nobody understands still counts as one, and `TestANamedPinentryThatCannotDrawIsNotReportedMissing` the message. What the container cannot show is the key reaching the agent: the same kernel-keyring limit as the row above | — no pinentry of any build is reachable on macOS: the dialog there is the system's own, drawn through `osascript`, and `pinentry` is not a name that platform's table offers |
| A graphical session whose first dialog announces a toolkit and then cannot draw (F29, F37, F12) | ✅ Real binary in a disposable **Wayland** image — sway on wlroots' headless backend, no X server and no Xwayland, zenity installed, no controlling terminal. `pinentry-gnome3` answers `GETINFO flavor` with `gnome3:curses`, so it is taken for a dialog, and then fails `GETPIN` with `Inappropriate ioctl for device` wherever the Gcr prompter it wants is not running. Against the previous build the user was shown **no window at all** and the log read `pinentry could not ask ... asking on the terminal instead`, then `no terminal available` — a person at a screen with a working dialog on it, asked nowhere. Now one `zenity :: Enter passphrase for id_a` window appears, is typed into, and the answer reaches the product; the log names pinentry as the one that could not ask and zenity as where the question went. The control is the same run with `gui_prompter = "zenity"`, which shows the dialog was always drawable there, and with `"none"`, which shows no window and the terminal answer. `TestADialogThatCannotDrawIsFollowedByOneThatCan` pins the chain, `TestANamedDialogThatCannotDrawGoesToTheTerminal` that a named dialog is still never substituted, `TestClosingTheFirstDialogDoesNotRaiseTheNext` that one closed window does not become the next, and `TestFallbackPrompter` the line the log writes. What this run cannot show is the key reaching the agent: the same kernel-keyring limit as the rows above | ⚠️ the chain and its rules run in the macOS job unchanged — they are platform-neutral and their tests carry no build tag. There is nothing to chain to there: the platform's table holds one dialog, so a dialog that cannot draw has only the terminal after it, which is what it already had |
| The dialog you are asked in is the configuration's to choose, and choosing one never costs you the prompt (F37) | ✅ Real binary, same image. `gui_prompter = "none"`: no dialog on a screen that had just shown one, and `sshakku config` names `config.toml` as what decided it. `gui_prompter = "kdialog"` with kdialog absent and both pinentry and zenity installed: no dialog appears at all and the log reads `gui_prompter names kdialog, which is not installed; asking on the terminal` — the case that would otherwise hand over a dialog the user never chose. Seen live too: a pinentry left unanswered logged `pinentry could not ask ... asking on the terminal instead`. `TestGraphicalPrompterHonoursTheConfiguration` pins the three answers at the factory, `TestResolveGUIPrompterFrom` the setting and the names each platform refuses, `TestFallbackPrompter` that a dialog that will not run reaches the terminal while a dismissed one is not asked again elsewhere | ⚠️ the setting, its refusals and the fallback rule are platform-neutral and run in the macOS job unchanged, and `TestNoGraphicalPrompterWhenTheUserRefusedOne` covers `"none"` there. No macOS job drives a real dialog a person answers — the same gap the row above records |
| `sshakku doctor --fix` recovers a dead own-agent | ✅ `TestDoctorDetectsAndFixesDeadOursAgent` | ✅ same |
| `doctor` shows the environment it runs in, and never a secret's value (F31) | ✅ `TestEnvironmentReportReadsTheShellItRunsIn` reads variables this shell really set; `TestASecretsValueNeverReachesTheReport` gives the handoff token a recognisable value and searches the whole rendered report for it; `TestFormatShowsEveryVariableItWasGiven` and `TestFormatSaysWhetherASecretIsSetAndNothingMore` pin the rendering. The leak the second one guards against is also structurally impossible: `SecretEnvVar` has no field for a value | ✅ same — the variables and the redaction are platform-neutral, and both halves run in the macOS job unchanged |
| `doctor --user` withholds what it cannot read instead of guessing (F31) | ✅ `TestAnUnreadableEnvironmentIsNotReportedAsUnset` — the cross-user report must not render an unread variable as `unset`, and must not conclude that SSH_AUTH_SOCK is unset or the askpass unwired from having never looked. Made to fail by disabling both guards, which restored all three false claims | ✅ same. Not driven through the real binary on either platform: doing so needs root plus a second user account with a live session, which no job has |
| `sshakku doctor --test-backend` proves the configured wallet for real (F15) | ✅ `test/bats/wallet-commands.bats` — the real binary against the wallet the configuration selects, asserting each of store, lookup and delete reported ok. It does not yet assert *which* wallet is named, so a report that named the wrong wallet would still pass here | ✅ same, against the real keychain |
| `sshakku forget` removes a stored passphrase and the entry is really gone (F9) | ✅ `test/bats/wallet-commands.bats` — seeded, forgotten, then the store is checked directly rather than the command's own report being believed | ✅ same, against the real keychain |
| `forget --all` leaves other programs' secrets alone (F27) | ✅ `test/bats/wallet-commands.bats` — but the stand-in store this suite uses cannot enumerate its entries, so nothing is listed there to delete and the case cannot be made to fail on Linux. What holds it up is `TestBitwardenListLeavesOtherItemsAlone` plus the real-CLI half of `TestBitwardenBackendRealAccount`, which stores an unprefixed item in a real vault and requires `List` not to report it | ✅ `test/bats/wallet-commands.bats` — another program's entry, seeded with a permissive access list so the keychain itself would allow the delete, survives `forget --all` and is not named in its output. Plus `TestKeychainBackendListLeavesOtherItemsAlone` |
| The entry name is the configuration's to choose, and saving, reading it back and forgetting it all go by it (F32) | ✅ `TestForgetRemovesTheEntryTheConfiguredPrefixNames` drives the real `forget` against a `config.toml` naming a prefix and checks the store itself, not the command's report; `TestOwnServicesGoesByThePrefixItIsGiven` and `TestBitwardenListGoesByTheBackendsOwnPrefix` pin the enumeration a shared store needs; `TestResolveServicePrefix` covers the setting, its default and the names it refuses. Made to fail by leaving the enumeration on the built-in constant, which had `List` report another program's entry and miss sshakku's own. Driven through the real binary on the `gnome-keyring` image as well: a key answered on a live terminal lands under the configured name and under no other, refills silently from it in a shell with no terminal at all, and after `sshakku forget <key>` the entry is gone and the next load has nothing left to find. What that run cannot show is the *nothing under any other name* half — the Linux default keeps its entries in a collection of its own, so what `forget --all` spares there it spares by collection, never by prefix; that half rests on the Bitwarden and keychain tests | ✅ same, plus `TestKeychainBackendListGoesByTheBackendsOwnPrefix` for the login keychain, where the prefix is the whole of what marks an item as sshakku's. That one runs only in the macOS job: it needs the darwin build, so it was written but never executed here |
| The compartment the entries are kept in is the configuration's to name, and a name belonging to the desktop's own wallet is refused (F33) | ✅ Driven through the real binary on the `gnome-keyring` image against a live Secret Service: a key answered on a real terminal lands in a collection labelled as configured, the collection SSHakku would otherwise have made stays empty, the next shell refills from it silently with no terminal at all. Then the container name is set to `default` — the desktop's own wallet — and `forget --all` is run against a keyring holding another program's secret seeded there beforehand: the log names the refused value and that secret is still in place, which is the case that can actually destroy something if the refusal ever stops working. `TestSecretServiceAddressesTheConfiguredContainer` pins alias and label together and every operation, `TestResolveSecretContainer` the setting and the names it refuses. Made to fail by leaving the backends reading their constants while the setting existed, which had every entry go to SSHakku's own compartment whatever was configured. The KeePassXC group is covered by a second run, on the `keepassxc` image against a real database file through the `"cli"` route: the group SSHakku creates carries the configured name, no `SSHakku` group is made beside it, the passphrase is read back out of that group after the key is dropped from the agent, and `sshakku forget` empties it — each read off `keepassxc-cli` itself rather than off sshakku's own report. `TestKeePassXCCLIUsesTheConfiguredContainer` pins the same four operations at the argv that crosses the process boundary | — (no Secret Service exists there, so there is no collection to name; the KeePassXC group half is platform-neutral and its unit tests run in the macOS job unchanged, though no macOS job drives a database through a configured group) |
| Which files count as the user's keys is the configuration's to state, and the report names the directory it read (F34) | ✅ Driven through the real binary on the `debian` image (no desktop, so the passphrase is asked for on a live terminal rather than fetched from a wallet, which is not what this is about). Four rounds against one agent: with no configuration a key called `work-github` is never asked for, never loaded and absent from `sshakku doctor`, whose heading names `~/.ssh`; with `key_patterns` naming it, the same key is prompted for, added to the agent and listed; with `key_dir` pointing at another directory and the widest rule a user can write, `key_patterns = ["*"]`, only the key there is loaded, the heading names that directory, and neither the report nor the session log mentions `config`, `known_hosts`, `authorized_keys` or a `.pub` — a file that is not a key is never offered to `ssh-add`; with `key_dir` naming a directory that is not there, `load-keys` fails and the log names the missing path instead of reporting no keys. `TestEnumeratorHonoursPatterns` and `TestEnumeratorNeverOffersWhatIsNotAKey` pin the rule, `TestResolveKeyDiscovery` the setting and the patterns it refuses, `TestKeyEnumeratorFromSettings` the single mapping both `load-keys` and `doctor` go through, `TestReportNamesTheKeyDirectoryItRead` the heading. Made to fail by leaving the directory and the name rule as the constants they were, which had every configuration load `~/.ssh/id_*` and the report name a directory it had not read. That run happened once, by hand; `test/bats/config-commands.bats` repeats it on every run of the shell suite, judging the selection on what SSHakku says it did — the session log naming the file it went and asked the wallet about, and the report listing it — rather than on the key reaching the agent, which turns on a passphrase handoff not every environment has and would leave the selection unexercised wherever it is missing. Made to fail one round at a time: the default rule widened to `["*"]`, `key_patterns` dropped from the mapping, and `key_dir` ignored so every configuration read `~/.ssh`. It also found the report naming no directory at all where the one it read held no key it recognised — the one moment a user needs the name — now pinned by `TestFormatNamesTheDirectoryItReadWhenItHeldNoKey`, made to fail by restoring the condition that printed the section only where there was a key to list | ✅ the same `test/bats/config-commands.bats`, run by the macOS job's `make test-bats`. Nothing in it is behind a build tag: the same rounds, the same fixtures, and a directory, a glob and a report on the other platform |
| What the configuration resolved to, and which file or variable decided each value, is one command away (F35) | ✅ Driven through the real binary on the `debian` image, which needs no agent, wallet or desktop for this: seven rounds against one configuration directory. A value saved in `config.toml` is reported as being in force and attributed to that file; a `config.d` drop-in setting the same key takes its place in the report, named; `SSHAKKU_KEY_LIFETIME` exported takes the file's place in turn; a duration no parser can read is shown as the default standing in for it, quoting what was refused, with nothing written to the session log to go and find; a drop-in that cannot be parsed is listed with its own error while every readable file still applies. `TestConfigReportsWhatIsInForce`, `TestConfigLetsTheEnvironmentSpeakForItself` and `TestConfigWithNothingConfiguredStillAnswers` pin the report, `TestExplainNamesWhereEachValueCameFrom` the origin of each value, `TestLoadSourcesReadsWhatALoginShellReads` the files and their order, and `TestEverySettingIsExplained` walks `File`'s own toml tags so a setting added without a line in the report fails the build. Made to fail by the command not existing: the report was the usage text and the exit code a usage error. `test/bats/config-commands.bats` repeats three of those rounds — the drop-in taking the file's place, the exported variable taking the drop-in's, and the refused duration shown beside the default standing in for it — on every run of the shell suite, made to fail the same way, by removing `config` from the command dispatch | ✅ the same `test/bats/config-commands.bats`, run by the macOS job's `make test-bats`: the same configuration directory, the same three rounds |
| The user's own `config.toml` can be edited in place, and what would have ignored the edit is said before they walk away (F36) | ✅ Same run: with no configuration at all, `--edit` creates a file naming every setting with none of them in force, hands that file to the editor, and what the editor saves is what `sshakku config` then reports. Editing a key a drop-in sets names the drop-in on the way out; a value that was refused is repeated; a file that no longer parses exits 1 and names itself, rather than being discarded whole at the next login. `TestConfigEditOpensTheUsersOwnFile` also asserts the editor was handed `config.toml` and nothing else, using `cp -t` as an editor that keeps a copy of what it was given. Made to fail by `--edit` being an unknown argument. What no run here shows is a person at a terminal editor: the editor is always a program that edits the file it is handed and exits. `test/bats/config-commands.bats` drives the four rounds through the installed binary with a fixture `$EDITOR` that records the arguments it was handed, so the promise that this file and no other is opened is checked from outside the process that opens it | ✅ the same `test/bats/config-commands.bats`, run by the macOS job's `make test-bats`, where `$EDITOR` and the file it is handed are what they are everywhere else |
| `sshakku forget` on an entry written by another program | ✅ — (the store that suite uses off darwin is a file the fixture writes, with no notion of who may touch it) | ✅ `test/bats/wallet-commands.bats` — a keychain item seeded without `-A`, so SSHakku is not on its access list: whatever it does, it may not report the passphrase forgotten while it is still there |
| A wait budget written in a configuration file is the one in force (F21, F35) | ✅ `TestTimeoutsWrittenInAFileAreTheOnesInForce` reads a real `config.toml` and a real drop-in the way a login shell does — `LoadSources` → `Merged` → `Resolve` — and requires the 45s and 9m written there, then requires the report to show 45s beside the file that set it. Neither value survived the merge before it: both settings are config-file only, so with no environment variable to reach them there was no way to set either, and `sshakku config` named the user's file beside the default it was really using. Every earlier test of these two built the `File` in memory and called `Resolve`, which is the seam the defect lived under; `TestMergeOtherWinsForEveryField` now fills the struct by reflection, so a field added without a merge clause fails the suite instead of disappearing quietly | ✅ same — the configuration layer is platform-neutral and runs unchanged in the macOS job |
| A wallet that puts its own unlock dialog in front of the user, and the wait that dialog is given (F21) | ✅ `TestAPromptIsGivenTheBudgetForAWaitOnAPerson` — a prompt that never completes is waited for exactly as long as the caller configured, and a caller that configured nothing gets a wait counted in the minutes a person takes rather than in seconds; `TestTheSecretServiceIsGivenBothConfiguredBudgets` requires the login path to hand the client both budgets, since neither reached it before. Made to fail by the values that were there: an unconfigured wait of 30s, and a client built with no budget at all. What no test here does is put a real dialog in front of a real person — the state was reached by hand instead, on a machine where the wallet was locked after a reboot: the dialog appeared, the thirty seconds ran out while it was being answered, and the passphrase was then asked for on the terminal | — (there is no freedesktop Secret Service on macOS, so no such prompt exists to bound; the Keychain's own approval dialog is the row below) |
| A keychain that makes SSHakku wait for someone to approve an access (F21) | — (no Linux backend here reaches its wallet this way) | ❌ not reachable from CI: SecurityAgent needs an interactive GUI session to put its dialog in, and a hosted runner has none — such a call fails there instead of waiting. Only a real desktop session can create the state, and one since has: on a Mac where the installed binary was no longer the one that had stored the items, the dialog came back and a lookup and a store each gave up while it was still on screen. What that reached is now pinned by `TestTheKeychainIsGivenTheBudgetForAWaitOnAPerson`, which requires the keychain to be handed `interactive_timeout` and not the shorter budget — the wait a person is on the other end of. The dialog itself remains unautomatable here |
| Headless (no TTY at all), vault already has the passphrase, silent load (F5) | ✅ `TestLoadKeysHeadlessVaultHit` | ✅ `test/bats/shell-plumbing.bats` — the shell suite runs on macOS, where the vault is the real keychain rather than the Linux kernel keyring that test's stand-in uses |
| A shell finds a working agent on a machine with no runtime directory of its own, from a long home (F1) | ✅ `test/bats/shell-plumbing.bats` — the logind directory taken away and the home given ninety characters, then the socket the hook names is asked to answer. Made to fail by that alone: before the session's own temporary directory was used, the address passed what the kernel accepts and no agent started | ✅ the same test, and there the scenario is not arranged but ordinary: no macOS login has a runtime directory |
| The same silent load from a home directory long enough to leave a socket address no room (F5) | — (the passphrase is handed over through the kernel keyring here, and a keyring description is not an address, so there is no length to run out of) | ✅ `test/bats/shell-plumbing.bats` — the row above, run from a home of ninety characters. Made to fail by the length alone: the home is moved with the wallet and the preferences that name it, so nothing else about the scenario differs |
| Live interactive TTY, vault empty, first-time passphrase prompt | ✅ `TestLoadKeysFirstTimePromptRealTerminal` — the loader runs in a child process holding a real pseudo-terminal as its controlling terminal, with an empty vault and no graphical prompter, while the test plays the user on the other end: the prompt reaches the terminal, the typed passphrase is never echoed back onto it, it is stored for next time, and the key lands in a real `ssh-agent` through the real out-of-band handoff | ✅ same |
| Wrong passphrase entered at the prompt | ✅ `TestLoadKeysWrongPassphraseRealTerminal` — the same live terminal, answered wrong every time: the user is asked exactly `MaxAttempts` times and no more, is told on the terminal that the key could not be loaded, and the key is marked as given up so later shells stop re-prompting; the rejected passphrase is neither echoed nor written to the vault, and the key stays out of the agent | ✅ same |
| The prompt is answered with nothing at all — Enter on its own (F8) | ✅ `TestLoadKeysEmptyAnswerRealTerminal` — the same live terminal, answered with an empty line every time: an empty answer is a wrong passphrase like any other, so the user is asked exactly `MaxAttempts` times, told once that the key could not be loaded, and the key is marked as given up; nothing empty reaches the vault and the key stays out of the agent. Only a real terminal can ask this: anywhere else "the user pressed Enter" and "there was nothing to read" are the same bytes. Made to fail against the previous loader, where the terminal showed `stash passphrase: invalid argument` after one Enter and no second prompt ever came | ✅ same run in the macOS job. The pre-fix code is what differed, not the promise: there the socket handoff accepted an empty payload and ssh-add did the rejecting, so the same keystroke cost one attempt on macOS and the whole key on Linux |
| The prompt is closed without being answered, with keys still queued behind it (F38) | ✅ Real binary in a disposable image with a display server, an empty wallet and three passphrase-protected keys, closed with the key a person presses to close a window they did not ask for. Five rounds against one agent: with no configuration the user is shown one window and no more, the log reads `passphrase prompt dismissed for id_a, asking about no further key this session`, the agent stays empty and no key is given up; running `load-keys` again with that state untouched asks about the first key again, which is what a later login would do; `on_dismiss = "skip"` shows exactly three windows, one per key, and still gives up on none; `on_dismiss = "retry"` shows three windows for the same key and then ends the way any key that never opened ends — `sshakku: could not load key id_a after 3 attempts`, and that key alone recorded as given up; `on_dismiss = "whenever"` is refused, `sshakku config` says so beside the file that wrote it, and the default applies. Nothing was told to the user in any round but the third, and no passphrase reached the wallet in any of them. This is one scenario the container's kernel keyring cannot blunt: every dialog was closed, so an empty agent is the promise rather than a limitation | ⚠️ the decision and the setting are platform-neutral and run in the macOS job unchanged — `TestLoadKeysDismissedDialogEndsTheAsking`, `TestLoadKeysDismissedDialogSkipsOnlyThatKey`, `TestLoadKeysDismissedDialogCountsAsAWrongAnswer`, and `TestOnDismissWrittenInAFileIsTheOneInForce` for the file it is written in. What no macOS job does is close a real dialog: the same gap as the two rows above, since nobody is at that runner's screen |
| Ctrl-D at a terminal prompt is the same gesture as closing the dialog (F38) | ✅ `TestLoadKeysDismissedOnRealTerminalIsNotAFailure` — the same live terminal as the rows above, with the input closed instead of answered: the user is asked once, is told of no failure, no key is given up, and nothing reaches the agent. Only a real terminal can ask this either, since Ctrl-D is not a character the program reads but something the line discipline turns into end-of-input. Made to fail against the previous loader, which reported a deliberate refusal as `sshakku: could not load key id_test: EOF` | ✅ same run in the macOS job — the terminal, the loader and the reading of end-of-input are the same code on both |
| Secret backend unresponsive or hangs | ✅ `TestSecretServiceUnresponsiveDaemon` — a live `SecretServiceBackend` round-trips, then `gnome-keyring-daemon` is SIGSTOP-frozen (still alive and still owning the bus name, so the bus cannot respawn it) and the next `Lookup`, bounded by the client's `CallTimeout`, must return an error promptly instead of blocking forever; `desktop-stack.yml` runs it in a throwaway container | ❌ what is missing is the scenario, not the deadline. The Keychain is reached through a synchronous Security.framework call, and every one of them is bounded — by `interactive_timeout`, since what the framework may be waiting on is a person approving the access — but a call already inside the framework cannot be cancelled, so what elapses ends SSHakku's waiting and not the call. There is no daemon to freeze from the outside the way this test does, and nothing here makes a *real* keychain hang: a client that neither answers nor fails stands in for one, in the row below |
| Secret-service daemon stopped/crashed mid-session (clean disconnect, not just slow) | ✅ `TestSecretServiceMidSessionFailure` (daemon-stopped) — a live `SecretServiceBackend` round-trips, then `gnome-keyring-daemon` is SIGKILLed (with its D-Bus activation file removed so the bus cannot respawn it) and the next `Lookup` must return promptly with an error, never a stale hit or a hang; `desktop-stack.yml` runs it in a throwaway container | — (Keychain has no comparable daemon to stop) |
| D-Bus session bus itself unreachable mid-session (lower-level than the daemon above) | ✅ `TestSecretServiceMidSessionFailure` (bus-unreachable) — same live round-trip, then the `dbus-daemon` session bus itself is SIGKILLed, severing the transport under the client, and the next `Lookup` must surface the broken connection promptly rather than hang | — |
| Real environment variables tampered (`SSH_AUTH_SOCK`, `SSH_ASKPASS`, `SSH_ASKPASS_REQUIRE`, `SSHAKKU_ASKPASS`, `SSHAKKU_HANDOFF_TOKEN`) | ✅ `TestTamperedEnvVarsHandledSafely` — real `os.Getenv` reads feeding the real `gatherReport`/`dispatch`/`askpass`: a hijacked or cleared `SSH_AUTH_SOCK` is flagged unreachable, a leftover `SSHAKKU_ASKPASS` marker changes nothing about what a command does, and a malformed `SSHAKKU_HANDOFF_TOKEN` redeems nothing from the real store. Clearing `SSH_ASKPASS`/`SSH_ASKPASS_REQUIRE` is what the rows below exercise end to end, since without them the wallet is never consulted. | ✅ same |
| A key gone from the agent is refilled from the wallet, in a shell already open, with no terminal and no graphical prompter (F6) | ✅ `test/bats/askpass-broker.bats` — the installed login hook is sourced, then a real `ssh-add` runs with no controlling terminal, so nothing can fall back to a prompt, and with no handoff token, so only the wallet can answer; the session log line naming the wallet is asserted, not merely the key reaching the agent | ✅ same, against the real keychain |
| The askpass broker is wired in a session with no graphical prompter (F6) | ✅ `test/bats/askpass-broker.bats` and `TestAskpassEnvHeadless` — with no `DISPLAY`/`WAYLAND_DISPLAY` and no `kdialog`, `askpass-env` still emits both exports, `SSH_ASKPASS` naming the helper installed beside the binary | ✅ same |
| A session with nobody to ask at all — no screen, and no terminal either, with nothing in the wallet to fall back on (F40) | ✅ `gnome-keyring-headless-scenario.sh` — in a login with no display, against a wallet that cannot be set up there, a `setsid` `ssh-add` with its input closed comes back at once rather than waiting, the session log says there was nobody to ask, and a key already in the agent is still usable from that same session. Made to fail by running it where the wallet does answer: there the passphrase comes from the wallet and nothing is ever left alone | ❌ not covered: this scenario reaches the state through a compartment that cannot be made, and the Keychain has none to make, so the same run does not transfer — the Mac form of it is a locked keychain in a session with no window server and no terminal, and nothing builds that today |
| A command SSHakku does not recognise, typed in a shell it is wired into, is reported as unknown and never taken for a request for a secret (F30) | ✅ `TestUnknownCommandIsNotASecretRequest` — the real `dispatch` run under the binary's own name, with the wiring's exports set and a terminal that would answer if it were asked: two spellings of a mistake (a flag, and a mistyped subcommand with an argument) are each named on stderr with the usage, nothing reaches stdout, and the terminal is never read. `test/bats/askpass-broker.bats` drives the installed binary in a shell that has sourced the real hook. What the shell test cannot show is the terminal going unread — it runs with no controlling terminal, where "did not ask" and "had nobody to ask" are indistinguishable — so that half of the promise rests on the Go test alone | ✅ same |
| A wallet that never answers — present but never returning — does not hold up an `ssh` or a login shell (F21), per backend that reaches its wallet by running a program | ✅ `test/bats/askpass-broker.bats`, once per backend × entry point: `secret-service` (blocked `secret-tool`, bounded by `command_timeout`) and `1password` (blocked `op`, bounded by `interactive_timeout`). `TestNoCommandBlocksIndefinitely` covers every external program this code runs, `bw` included — reached there by answering the master-password prompt Bitwarden asks for before it will run anything | ✅ `1password`; `secret-service` — (that backend does not exist on macOS). Bitwarden as on Linux |
| A wallet that never answers, where the wallet is **not** a program (F21) | — (every Linux backend reaches its wallet by running one, or over D-Bus with its own `CallTimeout`) | ✅ `TestKeychainGivesUpOnAKeychainThatNeverAnswers` (F21) drives all four operations against a keychain that neither answers nor fails — `Lookup`, `Store`, `Delete` and `List`, since `forget --all` waits on the last two exactly as `load-keys` waits on the first — and each gives up inside the budget it was given — `interactive_timeout`, since a keychain call may be waiting on a person approving it — instead of waiting. `TestWithDeadline` covers the giving-up itself, and runs on both platforms. What no test here does is make a *real* keychain hang: see the row above |
| The shell suite itself runs on this OS, against the real login hook and real ssh binaries | ✅ `make test-bats` in `debian.Dockerfile` | ✅ `make test-bats` in the `test-macos` job, after the throwaway keychain is set up |

The two live-terminal rows above are covered by the Go suite rather than by
`test/bats/shell-plumbing.bats`: that suite runs in a container with no
controlling terminal at all, while the Go tests allocate a pseudo-terminal
of their own. They need no daemon and no container, so they run on both
platforms in the ordinary test run.

## Init system (systemd/OpenRC/...)

Not a real axis: SSHakku has no runtime dependency on any init system or
service manager. `internal/diagnose` reads `/proc/<pid>/cgroup` to *label*
the launching systemd unit when one exists, purely for `doctor`'s
diagnostic output -- it changes no behavior and has nothing to fall back to
on a non-systemd host (OpenRC included).

## Keeping this current

Whenever a new OS/target, integration, session/display environment, or
recovery scenario is implemented (or a new gap is found), add or update its
row here in the same change.
