//go:build unix

package main

// editorFixtureName is the stand-in editor this system can run — see
// useEditor. A shell script, which is what an executable a program starts by
// name is here.
const editorFixtureName = "editor.sh"
