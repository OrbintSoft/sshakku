# Selects the 1Password CLI as the secret backend, so the wallet SSHakku
# consults is an external program on every platform rather than whichever
# implementation that platform happens to default to.
secret_backend = "1password"
onepassword_vault = "sshakku-test"
