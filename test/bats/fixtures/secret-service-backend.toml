# Selects the freedesktop Secret Service backend, reached through the
# `secret-tool` CLI. Named explicitly rather than left to the platform default
# so a test drives this backend because it says so, not because of the host.
secret_backend = "secret-service"
