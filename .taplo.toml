# taplo configuration. Some test fixtures are deliberately malformed (they
# exercise the parser's error path and are checked by the config package's Go
# tests), so they are excluded; the valid fixtures are linted and
# format-checked like any config.
exclude = ["**/testdata/malformed.toml", "**/testdata/confd-malformed/10-bad.toml"]
